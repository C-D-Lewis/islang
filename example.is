value counter is 0
value maximum is 100

counter is 10

task increment gets input
  return input + 1
end

log 'Hello, world!'

task main
  until counter equals maximum
    run increment with counter
    log '{counter}'
  end
  log 'complete!'
end

run main
